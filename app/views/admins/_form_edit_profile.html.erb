<%= simple_form_for @admin, :url => url_for("/admins/update_profile/#{@admin.id}"), :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :firstname %>
  <%= f.input :lastname %>
  <div class="form-actions">
    <%= f.button :submit, :class => 'btn-primary' %>
    <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                "/my_profile", :class => 'btn' %>
  </div>
<% end %>

